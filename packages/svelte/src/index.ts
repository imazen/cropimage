export {
  cropImage,
  setCropSelection,
  getCropSelection,
  type CropImageProps,
  type CropImageEvents,
} from './CropImage.js';

// Re-export core types
export type {
  CropRect,
  PadRect,
  CropSelection,
  AspectRatio,
  CropConfig,
} from '@imazen/crop-image-core';
