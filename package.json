{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "pnpm --filter @imazen/crop-image-demo dev"
  },
  "devDependencies": {
    "typedoc": "^0.28.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"],
    "overrides": {
      "minimatch": "^10.0.0"
    }
  }
}
